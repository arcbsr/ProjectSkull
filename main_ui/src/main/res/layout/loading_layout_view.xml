<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/lottie_save"
        android:layout_width="@dimen/dp_56"
        android:layout_height="@dimen/dp_56"
        android:layout_alignParentEnd="true"
        app:lottie_autoPlay="true"
        android:visibility="visible"
        app:lottie_fileName="loading_btn.json"
        app:lottie_loop="true"
        android:src="@drawable/ic_empty_state"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/emptyTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_chainStyle="packed" />

    <TextView
        android:id="@+id/emptyTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Loading, Please Wait..."
        android:textSize="@dimen/sp_20"
        android:layout_marginTop="@dimen/dp_16"
        style="@style/TextViewTitle"
        app:layout_constraintTop_toBottomOf="@id/lottie_save"
        app:layout_constraintBottom_toTopOf="@id/emptySubText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/emptySubText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="We couldn't find anything here right now."
        android:textSize="@dimen/sp_14"
        style="@style/TextViewSubtitle"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/emptyTextView"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
